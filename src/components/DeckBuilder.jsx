export default function DeckBuilder({ cards = [], selectedIds = [], onToggle }) {
  return (
    <div style={{ color: '#e8e0ff', padding: '16px 0' }}>
      <p style={{ opacity: 0.5, fontSize: 14 }}>DeckBuilder — coming in sprint 2</p>
    </div>
  );
}
