/**
 * Draws the match simulation onto an HTML Canvas element.
 * Full implementation coming in sprint 2.
 */

/**
 * @param {CanvasRenderingContext2D} ctx
 * @param {object} frame - Single frame from simulateMap output
 * @param {object} mapData - Entry from maps.json
 */
export function renderFrame(ctx, frame, mapData) {
  // ctx: CanvasRenderingContext2D
  // frame: single frame from simulateMap output
  // mapData: entry from maps.json
}
