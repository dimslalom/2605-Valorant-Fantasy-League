/**
 * Simulates a full 24-round map between two teams.
 * Full implementation coming in sprint 2.
 */

/**
 * @param {object[]} teamA - Array of 5 card objects
 * @param {object[]} teamB - Array of 5 card objects
 * @param {object} map - Entry from maps.json
 * @param {{ attackStrategy: string, defenseStrategy: string }} strategy
 * @returns {{ frames: object[], score: { a: number, b: number }, roundLog: object[] }}
 */
export function simulateMap(teamA, teamB, map, strategy) {
  // returns: { frames: [], score: { a: number, b: number }, roundLog: [] }
}
